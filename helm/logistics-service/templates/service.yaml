apiVersion: v1
kind: Service
metadata:
  name: {{ include "tracking-service.fullname" . }}
  labels:
    app: {{ include "tracking-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "tracking-service.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}